"""
Configuration package for Electronics Retail Chain Django project.

This package contains all the configuration files for the Django project
including settings, URLs, and WSGI/ASGI configurations.

Modules:
    settings: Django project settings configured with environment variables
    urls: URL routing configuration
    wsgi: WSGI configuration for production deployment
    asgi: ASGI configuration for async support
"""
