from django.apps import AppConfig


class NetworknodeConfig(AppConfig):
    """Конфигурация приложения networknode."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "networknode"
    verbose_name = "Узлы сети электроники"
